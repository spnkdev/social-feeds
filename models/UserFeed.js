/**
 * UserFeed
 * @typedef {Object} UserFeed
 * @property {string} type The feed type of feed..
 * @property {Object} feed The service feed.
 */

/**
 * @type {UserFeed}
 */
const userFeed = {
  type: '',
  feed: {},
};

module.exports = userFeed;
