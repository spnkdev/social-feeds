/**
 * Core
 * @typedef {Object} Core
 * @property {Function} getFeed Gets the feed from the selected service.
 */

/**
 * @type {Core}
 */
const core = {
  getFeed() { },
};


module.exports = core;
