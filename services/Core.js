/**
 * Core
 * @typedef {Object} CoreServices
 * @property {Function} getFeed The feed from the selected service.
 */

/**
 * @type {CoreServices}
 */
const core = {
  getFeed() {},
};

module.exports = core;
